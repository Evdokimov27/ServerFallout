messages:
  reloaded: "&aПлагин перезагружен!"

settings:
  interval: 10 #в тиках. 1 секунда = 20 тиков
  head:
    name: "Брони нет" #Установить none если не требуется
    owner: "hikrar" #Установить none если не требуется